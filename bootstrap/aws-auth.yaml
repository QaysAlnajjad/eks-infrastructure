apiVersion: v1
kind: ConfigMap
metadata:
  name: aws-auth
  namespace: kube-system
data:
  mapRoles: |
    # INFRA / CLUSTER ADMIN
    - rolearn: arn:aws:iam::127214183643:role/kubernetes-ci-infra-role
      username: ci:infra
      groups:
        - system:masters

    - rolearn: arn:aws:iam::127214183643:role/eks-node-role
      username: system:node:{{EC2PrivateDNSName}}
      groups:
        - system:bootstrappers
        - system:nodes

  mapUsers: |
    - userarn: arn:aws:iam::127214183643:user/admin-cli
      username: admin-cli
      groups:
        - system:masters
