apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: flask
  namespace: monitoring
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: flask
  namespaceSelector:
    matchNames:
      - default
  endpoints:
    - port: metrics
      path: /metrics
      interval: 15s

